#!/bin/sh

# Pause local music, but stop remote streams.
if mpc | grep -qF '[playing]'; then
  if mpc current -f '%file%' | egrep -q '^https?://'; then
    mpc stop
  else
    mpc pause
  fi
else
  mpc toggle
fi
