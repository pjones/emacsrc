% ============================================================================
\ProvidesPackage{pmadebase}

% ============================================================================
% \PhoneNumber -- Set the phone number used in title pages, etc.
\def\PhoneNumber#1{\gdef\@PhoneNumber{#1}}
\def\@PhoneNumber{(303) 219-0226}

% ============================================================================
% \EmailAddress -- Set the email address used in title pages, etc.
\def\EmailAddress#1{\gdef\@EmailAddress{#1}}
\def\@EmailAddress{pjones@devalot.com}

% ============================================================================
% \WebSite -- Set the URL of a website used in title pages, etc.
\def\WebSite#1{\gdef\@WebSite{#1}}
\def\@WebSite{http://devalot.com}

% ============================================================================
\usepackage{graphicx}

% ============================================================================
% Other nice fonts: Baskerville, Century Gothic, Cochin
%\def\SetMainFont#1{\gdef\@MainFont{#1}}
%\def\@MainFont{Georgia}

% ============================================================================
\usepackage{fontspec}
\usepackage{xunicode}
\usepackage{xltxtra}
%% \defaultfontfeatures{Scale=MatchLowercase}
%% \setmainfont[Mapping=tex-text]{\@MainFont}
%% \setsansfont[Mapping=tex-text]{Tahoma}
%% \setmonofont{Monaco}

% ============================================================================
\usepackage{titlesec}
\titleformat*{\section}{\Large\bfseries\sffamily}
\titleformat*{\subsection}{\large\bfseries\sffamily}
\titleformat*{\subsubsection}{\itshape\subsubsectionfont}

% ============================================================================
\usepackage{manfnt}
\renewcommand\labelitemi{\mantriangleright}

% ============================================================================
\usepackage{xcolor}
\usepackage{hyperref}

\definecolor{externallinkcolor}{HTML}{003399}
\definecolor{citelinkcolor}{HTML}{36823D}

\hypersetup{
  plainpages=false,
  colorlinks=true,
  linkcolor=black,
  filecolor=externallinkcolor,
  urlcolor=externallinkcolor,
  citecolor=citelinkcolor,
  pdfdisplaydoctitle=true,
  pdftoolbar=false,
  bookmarksopen=true
}

% ============================================================================
\usepackage{lastpage}
\usepackage{fancyhdr}

\let\OrigTableOfContents\tableofcontents
\renewcommand\tableofcontents{%
  \thispagestyle{empty}
  \OrigTableOfContents
  \newpage
  \pagestyle{fancy}
  \fancyheadoffset{\marginparsep+\marginparwidth}
  \fancyhf{}
  \fancyhead[L]{\leftmark}
  \fancyhead[R]{Page \thepage{} of \pageref{LastPage}}}

% ============================================================================
\let\OrigMakeTitle\maketitle
\renewcommand\maketitle{%
  \hypersetup{pdftitle={\@title}, pdfauthor={\@author}}
  \OrigMakeTitle}

% ============================================================================
% Add a link and a footnote.
\newcommand\flink[2]{\href{#1}{#2}\footnote{\href{#1}{#1}}}
