#!/usr/bin/env zsh

# set DISPLAY if it's not set
[ x$DISPLAY = x ] && export DISPLAY=:0

# Correct path
path=(/opt/local/bin /opt/local/lib/postgresql/bin /usr/local/texlive/2008/bin/universal-darwin $path)
