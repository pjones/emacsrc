# -*- mode: snippet -*-
# name: conf
# key: conf
# expand-env: ((yas-indent-line 'fixed))
# --
;;; ${1:`(file-name-base)`}.el -- Settings for \`${1:$(string-replace "-conf" "" yas-text)}' -*- lexical-binding: t -*-
;;
;;; Commentary:
;;
;;; Code:

(require '${1:$(string-replace "-conf" "" yas-text)})

(custom-set-variables
  '($0))

;;; $1 ends here
