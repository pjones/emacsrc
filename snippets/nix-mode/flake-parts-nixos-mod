# -*- mode: snippet -*-
# name: flake-parts-nixos-module
# key: nm
# expand-env: ((yas-indent-line 'fixed))
# --
flake.nixosModules.${1:name} = moduleWithSystem (
  { ... }:
  { ... }:
  {
    config = {
      $0
    };
  }
);
