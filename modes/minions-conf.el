;;; minions-conf.el -- Settings for `minions' -*- lexical-binding: t -*-
;;
;;; Commentary:
;;
;;; Code:

(require 'minions)

(custom-set-variables
 '(minions-mode-line-lighter "≡")
 '(minions-mode-line-delimiters nil)
 '(minions-prominent-modes '(flycheck-mode
                             flymake-mode)))

;;; minions-conf.el ends here
