;;; diff-hl-conf.el -- Settings for `diff-hl' -*- lexical-binding: t -*-
;;
;;; Commentary:
;;
;;; Code:

(require 'diff-hl)

(custom-set-variables
  '(diff-hl-side 'right)
  '(diff-hl-draw-borders nil))

;;; diff-hl-conf.el ends here
