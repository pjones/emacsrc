;;; url-cookie-conf.el -- Settings for `url-cookie' -*- lexical-binding: t -*-
;;
;;; Commentary:
;;
;;; Code:

(require 'url-cookie)

(custom-set-variables
 '(url-cookie-file (concat user-emacs-directory "url-cookies")))

;;; url-cookie-conf.el ends here
